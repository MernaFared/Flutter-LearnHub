import 'package:flutter/material.dart';

class BuildARContent extends StatelessWidget {
  const BuildARContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
