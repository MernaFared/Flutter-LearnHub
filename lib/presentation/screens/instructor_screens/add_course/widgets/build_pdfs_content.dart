import 'package:flutter/material.dart';

class BuildPdfContent extends StatelessWidget {
  const BuildPdfContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
