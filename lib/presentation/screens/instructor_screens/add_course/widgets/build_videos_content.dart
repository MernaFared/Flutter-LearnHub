import 'package:flutter/material.dart';

class BuildVideosContent extends StatelessWidget {
  const BuildVideosContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
